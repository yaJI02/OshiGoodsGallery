<%= link_to registration_post_stamp_path(post, stamp: 'loved'), id: 'loved-stamp-yet', class: 'reaction-stamp not-nice', data: { turbo_method: :post } do %>
  <span><%= t('enums.post_stamp.stamp.loved') %></span>
  <i class="bi bi-heart-fill reaction-stamp"></i>
<% end %>